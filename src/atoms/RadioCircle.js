import styled from '@emotion/styled';

const Styled = styled.div`
  background: #b14abd;
  border-radius: 50%;
  width: 0.8rem;
  height: 0.8rem;
`;

export default function RadioCircle() {
  return <Styled className="radio-circle" />;
}
