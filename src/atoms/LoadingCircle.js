import styled from '@emotion/styled';
import { motion } from 'framer-motion';

export default styled(motion.span)`
  width: 2rem;
  height: 2rem;
  background: #ddd;
  border-radius: 100%;
  margin: 0.5rem;
`;
